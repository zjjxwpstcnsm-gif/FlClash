# FlClash Auto

基于 [chen08209/FlClash](https://github.com/chen08209/FlClash) 的安卓自动节点切换版本，保留上游 GPL-3.0 许可证。此版本与上游没有官方关联。

## 使用

1. 安装 APK。近期安卓手机选择 `FlClash-Auto-arm64-v8a.apk`；旧款 32 位设备选择 `armeabi-v7a`。
2. 在配置页面导入自己的订阅链接，然后启动 VPN。
3. 在「工具 → 设置」开启「自动切换节点」，或在「应用」设置中开启同一个开关。
4. 在开关旁设置「最大可用延迟」，默认 200 ms，可输入 50–3000 ms；达到或超过阈值也视为失效。
5. 使用规则模式或全局模式。代理页的 `FlClash Auto (non-HK)` 显示当前自动选择的节点；`REJECT` 表示尚无通过检测的节点，后台仍在尝试恢复。

应用标识与原版不同，可以并存安装。同一时刻 Android 只能运行一个 VPN。订阅链接、节点密码和用户配置不会内置在 APK 或源码中。

## 行为

| 项目 | 行为 |
| --- | --- |
| 地区过滤 | 识别香港、港区、HK、Hong Kong、香港旗帜等名称；还校验检测响应中的实际出口国家，排除 HK |
| 检测 | 通过每个候选代理请求 `https://chatgpt.com/cdn-cgi/trace`；要求 HTTP 200 和有效的非 HK 出口国家 |
| 检测频率 | 当前节点每轮完成后 5 秒再检测，单次请求最多 3 秒；拨号失败可提前触发检测 |
| 节点选择 | 初次连接和故障恢复选择本轮通过检测的最低延迟节点，最多同时检测 10 个节点 |
| 延迟阈值 | 当前节点和候选节点均须严格低于设置值；默认 200 ms，修改后立即重新检测；全部超标时等待恢复 |
| 稳定性 | 正常连接每 60 秒比较候选节点；新节点至少快 50 ms 才切换；刚失效的当前节点暂避 30 秒 |
| 后台运行 | 检测由 Go 核心运行；不依赖 Flutter 界面计时器，停止 VPN 时取消检测 |
| 路由 | 原来指向代理或代理组的规则转到自动组；显式 DIRECT/REJECT 规则保留；ChatGPT/OpenAI 相关域名优先走自动组；全局模式固定自动组 |
| 全部失效 | 保持 VPN 服务，代理流量等待恢复，不退回香港或 DIRECT；候选节点恢复后自动继续 |
| 连接处理 | 故障时关闭失败节点上经过自动组的连接，让应用重连；正常延迟优化只影响新连接 |
| 开关关闭 | 恢复订阅原有规则和手动选点；订阅原始文件不被改写 |

HTTP 探测通过表示该域名和出口可达，不保证 ChatGPT 登录、账号权限、模型调用或 IP 风控全部可用。若检测接口本身被阻断，节点也会被排除。节点越多，完整一轮检测时间越长。切换不能迁移已有 TCP 会话，正在生成的回答可能需要重试。

## 构建与签名

开发分支为 `auto-failover`。在 fork 的 Actions 中启用工作流后，推送该分支或手动运行 `Android Auto Failover` 即可：生成代码 → 静态分析 → Flutter 测试 → Go 测试 → 构建 APK → 发布预览版本。

工作流使用 Flutter 3.44.4、Go 1.26.4、JDK 17、Android NDK r28c。每个 APK 附带 SHA-256 校验和，发布标签对应生成代码后的源码提交。

当前无专用发布密钥时，沿用上游的 release 构建加 debug 签名回退，包名为 `com.follow.clash.autofailover.dev`。这是测试分发包，不适合商店发布；不同构建机的 debug 签名可能不同，升级可能需要先导出配置并卸载旧测试包。正式长期分发应使用自己保存的固定 keystore，按 `android/app/build.gradle.kts` 的签名字段配置。

本 fork 未配置上游 Firebase 项目，崩溃统计在没有 Firebase 配置时安全跳过。自动检查更新默认关闭，避免安装上游版本覆盖定制功能。

## 验证

- 自动测试覆盖节点失效、全部失效、自动恢复、香港过滤、实际出口校验、抖动、订阅刷新和取消过期检测。
- 路由测试覆盖保留直连/拒绝规则、子规则、候选为空、节点初始未验证和阻止手动固定自动组。
- Flutter 测试覆盖开关切换、旧配置兼容及设置序列化。
- Android 真机上仍需验证：授权 VPN、导入自己的订阅、断开当前节点、锁屏、Wi-Fi/蜂窝切换、系统回收服务、从通知或快捷开关启动。

发布成功不等于已完成上述真机验收；请以本次交付说明中的实际验证结果为准。
